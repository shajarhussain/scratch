{
  "name": "fyp",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "render-build": "cd backend && npm install --omit=dev && cd ../frontend && npm install && npm run build",
    "render-start": "node backend/server.js",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongoose": "^9.0.0"
  }
}
