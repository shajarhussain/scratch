{
  "name": "fyp-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "render-build": "npm --prefix backend install --omit=dev && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "render-start": "node backend/server.js",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
